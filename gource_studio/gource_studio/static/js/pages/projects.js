"use strict";

// Import globals setup
window.App = window.App || {};
window.App.pages = window.App.pages || {};
window.App.pages.projects = {};


/**
 * Projects (list) page
 */
App.pages.projects.init = function(page_options) {
    // Initialize page defaults
    page_options = page_options || {};
};
